/**
 * @deprecated Import from `@/components/system` instead.
 * Thin re-export so existing ProductPreview imports keep working.
 */
export {
  WorkflowTimeline as Timeline,
  WorkflowTimeline,
  type WorkflowStep as TimelineItem,
  type WorkflowStepStatus as TimelineItemStatus,
  type WorkflowTimelineProps as TimelineProps,
} from "@/components/system/WorkflowTimeline"
