# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node
node_modules/

# Environment and editor files
.env
.env.*
!.env.example
.judge_ro_password
.judge_ro_database_url
docs/judge_walkthrough_artifacts/
docs/envexamp.md
.vscode/
.local_secrets/
backend/.local_secrets/

# SAM / local build output (never commit)
infra/sam/.aws-sam/
**/.aws-sam/

# OS files
.DS_Store
Thumbs.db
.tmp_schema.json
.tmp_*.json

# Playwright MCP session artefacts (console logs / page snapshots)
.playwright-mcp/
debug-*.log

# `npx skills add` writes .claude/skills/* as ABSOLUTE symlinks into this
# machine's checkout path, so they are broken for every other clone. The real
# vendored content lives in .agents/skills/ (committed); regenerate the
# symlinks locally with `npx skills add cockroachlabs/cockroachdb-skills`.
.claude/

# Original hero-video export (54MB gif + 17MB mp4) — the compressed,
# web-ready derivative lives in frontend/oracle/apps/web/public/hero-demo.mp4
# and is what actually ships. Keep the source out of git; too large.
/Untitled design (8).gif
/Untitled design (8).mp4
